spring:
  jpa:
    show-sql: true  # 실행된 SQL 출력
    properties:
      hibernate:
        format_sql: true # SQL 출력 포맷팅 (예쁘게)
logging:
  level:
    org.hibernate.type.descriptor.sql: trace # 바인딩 파라미터를 포함한 SQL 출력